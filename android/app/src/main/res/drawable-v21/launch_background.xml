<?xml version="1.0" encoding="utf-8"?>
<!-- The window Android paints before Flutter's first frame.

     It used to be `?android:colorBackground`, which is white in the light theme and
     black in the dark one - neither of which is the page's actual colour, so the
     start-up visibly jumped. It is now the page colour on both themes. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
