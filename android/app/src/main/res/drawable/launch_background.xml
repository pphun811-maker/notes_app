<?xml version="1.0" encoding="utf-8"?>
<!-- The window Android paints before Flutter's first frame (below API 21 only;
     drawable-v21 overrides this on every version Flutter supports).

     It used to be plain white, which flashed before the grey page. It is now the
     page's own colour so the start-up is seamless. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
